SELECT * FROM USER_ AS U
INNER JOIN ADDRES AS A ON A.USERID=U.ID
WHERE U.ID IN (100,101,102)

SELECT * FROM USER_ AS U
LEFT JOIN ADDRES AS A ON A.USERID=U.ID
WHERE U.ID IN (100,101,102)

SELECT * FROM USER_ AS U
RIGHT JOIN ADDRES AS A ON A.USERID=U.ID
WHERE U.ID IN (100,101,102)

SELECT * FROM USER_ AS U
FULL JOIN ADDRES AS A ON A.USERID=U.ID
WHERE U.ID IN (100,101,102)

SELECT * FROM ADDRES WHERE USERID IN (100,101,102)
DELETE FROM ADDRES WHERE USERID=102